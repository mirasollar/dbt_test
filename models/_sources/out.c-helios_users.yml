---
version: 2
sources:
  - name: out.c-helios_users
    freshness:
      warn_after:
        count: 1
        period: day
    database: '{{ env_var("DBT_KBC_DEV_DATABASE") }}'
    schema: out.c-helios_users
    loaded_at_field: '"_timestamp"'
    tables:
      - name: helios_users_input
        quoting:
          database: true
          schema: true
          identifier: true
        columns: []
