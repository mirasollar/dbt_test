---
version: 2
sources:
  - name: out.c-multiplikator_zarizeni
    freshness:
      warn_after:
        count: 1
        period: day
    database: '{{ env_var("DBT_KBC_DEV_DATABASE") }}'
    schema: out.c-multiplikator_zarizeni
    loaded_at_field: '"_timestamp"'
    tables:
      - name: multiplikator_zarizeni_out
        quoting:
          database: true
          schema: true
          identifier: true
        columns: []
