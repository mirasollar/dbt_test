---
version: 2
sources:
  - name: out.c-prevody-vlastniku
    freshness:
      warn_after:
        count: 1
        period: day
    database: '{{ env_var("DBT_KBC_DEV_DATABASE") }}'
    schema: out.c-prevody-vlastniku
    loaded_at_field: '"_timestamp"'
    tables:
      - name: prevod_vlastnictvi_final
        quoting:
          database: true
          schema: true
          identifier: true
        columns: []
