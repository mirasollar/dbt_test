---
version: 2
sources:
  - name: out.c-test-inkrementu
    freshness:
      warn_after:
        count: 1
        period: day
    database: '{{ env_var("DBT_KBC_DEV_DATABASE") }}'
    schema: out.c-test-inkrementu
    loaded_at_field: '"_timestamp"'
    tables:
      - name: test_inkrement
        quoting:
          database: true
          schema: true
          identifier: true
        columns:
          - name: '"id"'
            tests:
              - unique
              - not_null
