---
version: 2
sources:
  - name: in.c-Email_profi_clients
    freshness:
      warn_after:
        count: 1
        period: day
    database: '{{ env_var("DBT_KBC_DEV_78_DATABASE") }}'
    schema: in.c-adhoc
    loaded_at_field: '"_timestamp"'
    tables:
      - name: Email_profi_clients
        quoting:
          database: true
          schema: true
          identifier: true
        columns: []
