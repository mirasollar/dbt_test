---
version: 2
sources:
  - name: out.c-client-domain
    freshness:
      warn_after:
        count: 1
        period: day
    database: '{{ env_var("DBT_KBC_DEV_DATABASE") }}'
    schema: out.c-client-domain
    loaded_at_field: '"_timestamp"'
    tables:
      - name: CLIENT_ENRICHMENT_RUS_DOMAIN
        quoting:
          database: true
          schema: true
          identifier: true
        columns: []
