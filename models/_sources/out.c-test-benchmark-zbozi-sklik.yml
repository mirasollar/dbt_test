---
version: 2
sources:
  - name: out.c-test-benchmark-zbozi-sklik
    freshness:
      warn_after:
        count: 1
        period: day
    database: '{{ env_var("DBT_KBC_DEV_DATABASE") }}'
    schema: out.c-test-benchmark-zbozi-sklik
    loaded_at_field: '"_timestamp"'
    tables:
      - name: statistika_mozne_velikosti_datasetu
        quoting:
          database: true
          schema: true
          identifier: true
        columns: []
