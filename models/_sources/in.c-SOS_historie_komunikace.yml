---
version: 2
sources:
  - name: in.c-SOS_historie_komunikace
    freshness:
      warn_after:
        count: 1
        period: day
    database: '{{ env_var("DBT_KBC_DEV_78_DATABASE") }}'
    schema: out.c-rs-main
    loaded_at_field: '"_timestamp"'
    tables:
      - name: historie_komunikace
        quoting:
          database: true
          schema: true
          identifier: true
        columns:
          - name: '"fake_identifikator"'
            tests:
              - unique
              - not_null
