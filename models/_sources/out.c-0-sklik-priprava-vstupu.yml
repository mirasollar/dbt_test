---
version: 2
sources:
  - name: out.c-0-sklik-priprava-vstupu
    freshness:
      warn_after:
        count: 1
        period: day
    database: '{{ env_var("DBT_KBC_DEV_DATABASE") }}'
    schema: out.c-0-sklik-priprava-vstupu
    loaded_at_field: '"_timestamp"'
    tables:
      - name: sklik_userid_aggregated
        quoting:
          database: true
          schema: true
          identifier: true
        columns: []
      - name: sklik_zoneid_aggregated
        quoting:
          database: true
          schema: true
          identifier: true
        columns: []
