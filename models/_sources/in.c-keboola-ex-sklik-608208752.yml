---
version: 2
sources:
  - name: in.c-keboola-ex-sklik-608208752
    freshness:
      warn_after:
        count: 1
        period: day
    database: '{{ env_var("DBT_KBC_DEV_DATABASE") }}'
    schema: in.c-keboola-ex-sklik-608208752
    loaded_at_field: '"_timestamp"'
    tables:
      - name: accounts
        quoting:
          database: true
          schema: true
          identifier: true
        columns:
          - name: '"userId"'
            tests:
              - unique
              - not_null
