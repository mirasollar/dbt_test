---
version: 2
sources:
  - name: in.c-seznam_sbazar
    freshness:
      warn_after:
        count: 1
        period: day
    database: '{{ env_var("DBT_KBC_DEV_DATABASE") }}'
    schema: in.c-seznam_sbazar
    loaded_at_field: '"_timestamp"'
    tables:
      - name: seznam_sbazar_daily
        quoting:
          database: true
          schema: true
          identifier: true
        columns: []
