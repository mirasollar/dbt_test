---
version: 2
sources:
  - name: in.c-project-list-gd
    freshness:
      warn_after:
        count: 1
        period: day
    database: '{{ env_var("DBT_KBC_DEV_78_DATABASE") }}'
    schema: in.c-keboola-ex-onedrive-326509425
    loaded_at_field: '"_timestamp"'
    tables:
      - name: project-list-gd
        quoting:
          database: true
          schema: true
          identifier: true
        columns: []
