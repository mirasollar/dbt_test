---
version: 2
sources:
  - name: in.c-klient-domain
    freshness:
      warn_after:
        count: 1
        period: day
    database: '{{ env_var("DBT_KBC_DEV_DATABASE") }}'
    schema: in.c-klient-domain
    loaded_at_field: '"_timestamp"'
    tables:
      - name: CLIENT_ID
        quoting:
          database: true
          schema: true
          identifier: true
        columns: []
