---
version: 2
sources:
  - name: out.c-sklik-zone-weeks
    freshness:
      warn_after:
        count: 1
        period: day
    database: '{{ env_var("DBT_KBC_DEV_DATABASE") }}'
    schema: out.c-sklik-zone-weeks
    loaded_at_field: '"_timestamp"'
    tables:
      - name: sklik_source_feed_daily
        quoting:
          database: true
          schema: true
          identifier: true
        columns: []
