---
version: 2
sources:
  - name: in.c-cileni_sklik
    freshness:
      warn_after:
        count: 1
        period: day
    database: '{{ env_var("DBT_KBC_DEV_78_DATABASE") }}'
    schema: out.c-ostatni
    loaded_at_field: '"_timestamp"'
    tables:
      - name: zoky_vystup_gd
        quoting:
          database: true
          schema: true
          identifier: true
        columns: []
