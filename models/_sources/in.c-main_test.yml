---
version: 2
sources:
  - name: in.c-main_test
    freshness:
      warn_after:
        count: 1
        period: day
    database: '{{ env_var("DBT_KBC_DEV_DATABASE") }}'
    schema: in.c-main_test
    loaded_at_field: '"_timestamp"'
    tables:
      - name: rtb_appnexus
        quoting:
          database: true
          schema: true
          identifier: true
        columns: []
