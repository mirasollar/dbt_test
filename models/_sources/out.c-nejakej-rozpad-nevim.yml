---
version: 2
sources:
  - name: out.c-nejakej-rozpad-nevim
    freshness:
      warn_after:
        count: 1
        period: day
    database: '{{ env_var("DBT_KBC_DEV_DATABASE") }}'
    schema: out.c-nejakej-rozpad-nevim
    loaded_at_field: '"_timestamp"'
    tables:
      - name: test_vystup_nested_json
        quoting:
          database: true
          schema: true
          identifier: true
        columns: []
