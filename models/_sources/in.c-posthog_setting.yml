---
version: 2
sources:
  - name: in.c-posthog_setting
    freshness:
      warn_after:
        count: 1
        period: day
    database: '{{ env_var("DBT_KBC_DEV_DATABASE") }}'
    schema: in.c-posthog_setting
    loaded_at_field: '"_timestamp"'
    tables:
      - name: user
        quoting:
          database: true
          schema: true
          identifier: true
        columns: []
