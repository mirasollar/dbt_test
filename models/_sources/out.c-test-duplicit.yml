---
version: 2
sources:
  - name: out.c-test-duplicit
    freshness:
      warn_after:
        count: 1
        period: day
    database: '{{ env_var("DBT_KBC_DEV_DATABASE") }}'
    schema: out.c-test-duplicit
    loaded_at_field: '"_timestamp"'
    tables:
      - name: test_duplicit
        quoting:
          database: true
          schema: true
          identifier: true
        columns: []
