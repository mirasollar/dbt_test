---
version: 2
sources:
  - name: in.c-multiplikator_zarizeni
    freshness:
      warn_after:
        count: 1
        period: day
    database: '{{ env_var("DBT_KBC_DEV_78_DATABASE") }}'
    schema: in.c-db_query_sklik
    loaded_at_field: '"_timestamp"'
    tables:
      - name: multiplikator_zarizeni
        quoting:
          database: true
          schema: true
          identifier: true
        columns: []
