---
version: 2
sources:
  - name: out.c-oath_hash_adresy
    freshness:
      warn_after:
        count: 1
        period: day
    database: '{{ env_var("DBT_KBC_DEV_DATABASE") }}'
    schema: out.c-oath_hash_adresy
    loaded_at_field: '"_timestamp"'
    tables:
      - name: ciselnik_hash_id_emailu
        quoting:
          database: true
          schema: true
          identifier: true
        columns: []
