---
version: 2
sources:
  - name: in.c-manual_codebooks
    freshness:
      warn_after:
        count: 1
        period: day
    database: '{{ env_var("DBT_KBC_DEV_DATABASE") }}'
    schema: in.c-manual_codebooks
    loaded_at_field: '"_timestamp"'
    tables:
      - name: COMMON_DATASET_FEEDTAG
        quoting:
          database: true
          schema: true
          identifier: true
        columns: []
      - name: SKLIK_COMMON_DEVICE_TYPE
        quoting:
          database: true
          schema: true
          identifier: true
        columns: []
