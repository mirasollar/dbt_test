---
version: 2
sources:
  - name: in.advertising-sklik
    freshness:
      warn_after:
        count: 1
        period: day
    database: '{{ env_var("DBT_KBC_DEV_DATABASE") }}'
    schema: in.advertising-sklik
    loaded_at_field: '"_timestamp"'
    tables:
      - name: COMPLETE_OVERVIEW
        quoting:
          database: true
          schema: true
          identifier: true
        columns: []
      - name: WINRATE_OVERVIEW
        quoting:
          database: true
          schema: true
          identifier: true
        columns: []
