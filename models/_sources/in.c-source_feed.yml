---
version: 2
sources:
  - name: in.c-source_feed
    freshness:
      warn_after:
        count: 1
        period: day
    database: '{{ env_var("DBT_KBC_DEV_DATABASE") }}'
    schema: in.c-source_feed
    loaded_at_field: '"_timestamp"'
    tables:
      - name: source_feed
        quoting:
          database: true
          schema: true
          identifier: true
        columns: []
