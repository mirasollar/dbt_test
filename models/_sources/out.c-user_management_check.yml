---
version: 2
sources:
  - name: out.c-user_management_check
    freshness:
      warn_after:
        count: 1
        period: day
    database: '{{ env_var("DBT_KBC_DEV_DATABASE") }}'
    schema: out.c-user_management_check
    loaded_at_field: '"_timestamp"'
    tables:
      - name: kontrola_user_managementu
        quoting:
          database: true
          schema: true
          identifier: true
        columns: []
