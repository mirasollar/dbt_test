---
version: 2
sources:
  - name: in.c-dates_table
    freshness:
      warn_after:
        count: 1
        period: day
    database: '{{ env_var("DBT_KBC_DEV_78_DATABASE") }}'
    schema: in.c-static
    loaded_at_field: '"_timestamp"'
    tables:
      - name: dates
        quoting:
          database: true
          schema: true
          identifier: true
        columns: []
